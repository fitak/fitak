#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../csswring/bin/csswring" "$@"
  ret=$?
else 
  node  "$basedir/../csswring/bin/csswring" "$@"
  ret=$?
fi
exit $ret
